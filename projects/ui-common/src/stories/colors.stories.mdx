import { moduleMetadata } from '@storybook/angular'
import { Story, Meta, ColorItem, ColorPalette } from '@storybook/addon-docs/blocks'
// import { linkTo } from '@storybook/addon-links'

import { ColorDisp } from './mdx/components/color-preview'

# Colors

<Meta title="Docs|Colors" />

## Theme Colors

The theme colors should work with any bootstrap theme class, such as '.text-primary' and '.bg-primary'

<ColorPalette>
  <ColorDisp title='primary' colorVars={['--primary']} />
  <ColorDisp title='secondary' colorVars={['--secondary']} />
  <ColorDisp title='success' colorVars={['--success']} />
  <ColorDisp title='info' colorVars={['--info']} />
  <ColorDisp title='warning' colorVars={['--warning']} />
  <ColorDisp title='danger' colorVars={['--danger']} />
  <ColorDisp title='light' colorVars={['--light']} />
  <ColorDisp title='dark' colorVars={['--dark']} />
</ColorPalette>
