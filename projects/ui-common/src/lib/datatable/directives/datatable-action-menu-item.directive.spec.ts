import { DatatableActionMenuItemDirective } from './datatable-action-menu-item.directive'

describe('DatatableActionMenuItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DatatableActionMenuItemDirective()
    expect(directive).toBeTruthy()
  })
})
