import { moduleMetadata } from '@storybook/angular'
import { Story, Meta, Preview } from '@storybook/addon-docs/blocks'

import { DocAlert } from '../../../stories/mdx/components/index'

import { BrowserAnimationsModule } from '@angular/platform-browser/animations'

import { TheSeamSharedModule } from '../shared.module'

<Meta title="Directives/HoverClass" decorators={[moduleMetadata({
  imports: [
    BrowserAnimationsModule,
    TheSeamSharedModule
  ]
})]} />

# HoverClass

Changes a css class on mouse pointer hover.

<DocAlert>Only use this if css `:hover` can't be used, because css is more accurate and better performing.</DocAlert>

<Preview>
  <Story name="Example" height="100px">
    {{
      template: `<div style="padding: 25px;" class="text-center bg-primary" seamHoverClass="bg-success">Hover me</div>`,
      props: { },
    }}
  </Story>
</Preview>
