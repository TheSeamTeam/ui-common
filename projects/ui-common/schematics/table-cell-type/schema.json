{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsTableCellType",
  "title": "Table Cell Type Schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the table cell type.",
      "type": "string"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the table cell type.",
      "visible": false
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
   },
  "required": [
    "name"
  ]
}
